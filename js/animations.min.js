const controller = new ScrollMagic.Controller(),
  revealDurex = document.getElementsByClassName("es-kv__img-durex");
for (let a = 0; a < revealDurex.length; a++)
  new ScrollMagic.Scene({
    triggerElement: revealDurex[a],
    offset: 50,
    triggerHook: 0.9,
    reverse: !1,
  })
    .setClassToggle(revealDurex[a], "visible")
    .addTo(controller);
const revealWhy = document.getElementsByClassName("es-features__title");
for (let a = 0; a < revealWhy.length; a++)
  new ScrollMagic.Scene({
    triggerElement: revealWhy[a],
    offset: 50,
    triggerHook: 0.9,
    reverse: !1,
  })
    .setClassToggle(revealWhy[a], "visible")
    .addTo(controller);
const revealFeatures = document.getElementsByClassName("es-features__feature");
for (let a = 0; a < revealFeatures.length; a++)
  new ScrollMagic.Scene({
    triggerElement: revealFeatures[a],
    offset: 50,
    triggerHook: 0.9,
    reverse: !1,
  })
    .setClassToggle(revealFeatures[a], "visible")
    .addTo(controller);
const revealBenefitsLogo = document.getElementsByClassName("es-benefits__logo");
for (let a = 0; a < revealBenefitsLogo.length; a++)
  new ScrollMagic.Scene({
    triggerElement: revealBenefitsLogo[a],
    offset: 50,
    triggerHook: 0.9,
    reverse: !1,
  })
    .setClassToggle(revealBenefitsLogo[a], "visible")
    .addTo(controller);
const revealBenefitsTitle =
  document.getElementsByClassName("es-benefits__title");
for (let a = 0; a < revealBenefitsTitle.length; a++)
  new ScrollMagic.Scene({
    triggerElement: revealBenefitsTitle[a],
    offset: 50,
    triggerHook: 0.9,
    reverse: !1,
  })
    .setClassToggle(revealBenefitsTitle[a], "visible")
    .addTo(controller);
const revealBenefitsText = document.getElementsByClassName("es-benefits__text");
for (let a = 0; a < revealBenefitsText.length; a++)
  new ScrollMagic.Scene({
    triggerElement: revealBenefitsText[a],
    offset: 50,
    triggerHook: 0.9,
    reverse: !1,
  })
    .setClassToggle(revealBenefitsText[a], "visible")
    .addTo(controller);
const revealBenefitsLink = document.getElementsByClassName("es-benefits__link");
for (let a = 0; a < revealBenefitsLink.length; a++)
  new ScrollMagic.Scene({
    triggerElement: revealBenefitsLink[a],
    offset: 50,
    triggerHook: 0.9,
    reverse: !1,
  })
    .setClassToggle(revealBenefitsLink[a], "visible")
    .addTo(controller);
const revealBenefits = document.getElementsByClassName("es-benefits__benefit");
for (let a = 0; a < revealBenefits.length; a++)
  new ScrollMagic.Scene({
    triggerElement: revealBenefits[a],
    offset: 50,
    triggerHook: 0.9,
    reverse: !1,
  })
    .setClassToggle(revealBenefits[a], "visible")
    .addTo(controller);

const chartContainer = document.querySelector(".es-info__chart-container");
const chartContainerScene = new ScrollMagic.Scene({
  triggerElement: chartContainer,
  offset: 500,
  triggerHook: 1,
  reverse: false,
})
  .setClassToggle(chartContainer, "animate")
  .addTo(controller);
